/* 
represents a single boid object in the simulation.
 - has position and velocity
 */

 
#pragma once


struct Boid {
    float x, y;       // position
    float vx, vy;     // velocity
};

